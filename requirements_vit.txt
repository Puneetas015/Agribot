# ── Existing packages (already installed) ────────────────────
Django>=4.2,<5.0
Pillow>=10.0
requests>=2.31
scikit-learn>=1.3
joblib>=1.3
numpy>=1.24
pandas>=2.0

# ── NEW: ViT via HuggingFace Transformers ─────────────────────
transformers>=4.38.0        # ViTForImageClassification
torch>=2.1.0                # PyTorch (CPU version is fine)
torchvision>=0.16.0         # Image transforms
timm>=0.9.0                 # Required by some ViT configs
accelerate>=0.27.0          # Faster training

# ── OPTIONAL: TensorFlow (keep for CNN fallback) ─────────────
# tensorflow-cpu>=2.13      # Uncomment if you want CNN fallback
